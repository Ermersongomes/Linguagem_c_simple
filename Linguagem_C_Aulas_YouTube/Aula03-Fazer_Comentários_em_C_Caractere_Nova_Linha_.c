#include <stdio.h>

int main()
{
    // Este é um comentário de linha única

    printf("Usando comentários dentro do código. \n"); // O \n servira para pular um linha no código.

    /* Este é um comentário de múltiplas linhas.
   Ele pode se estender por várias linhas.
   Tudo entre ele será ignorado pelo compilador. */

    return 0;
}
