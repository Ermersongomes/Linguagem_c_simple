#include <stdio.h>
//este programa gera a sen=guencia de fibonacci
int main(){
    //declaração das variáveis

    int primeiro  = 0 , segundo = 1, proximo;
    printf("%i, ", primeiro);
    printf("%i, ", segundo);
    proximo = primeiro + segundo;
    printf("%i, ", proximo);
    //atualizando as variaveis andando para frente.
    
}